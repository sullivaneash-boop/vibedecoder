import React from 'react'
import ReactDOM from 'react-dom/client'
import VibeDecoder from './VibeDecoder'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <VibeDecoder />
  </React.StrictMode>,
)
